"""La SyntaxError arrive lorsqu'on ne respecte pas la syntaxe de Python."""

# renvoie une SyntaxError car on a mis une majuscule à For au lieu de for
For i in range(10):

# ici on a oublié les ":" à la fin de la structure conditionnelle
if utilisateur == "admin"

# ici on utilise le mot réservé "continue" (sans majuscule) qui ne peut pas être utilisé comme nom de variable
Continue = "Oui"

# ici on oublie de fermer les guillemets
print("Bonjour " + "tout le monde !)